options:
  organization: acme-co
  hostname: https://be-create-se.k8s.insights.fairwinds.com

  # You may need to set this to 'main'
  baseBranch: main

manifests:
  yaml:
    - ./passing.yaml
    - ./failing.yaml

reports:
  trivy:
    enabled: false

options:
  organization: acme-co
  hostname: http://host.docker.internal:3000

  # You may need to set this to 'main'
  baseBranch: main

manifests:
  yaml:
    - ./passing.yaml
    - ./failing.yaml

reports:
  trivy:
    enabled: false

options:
  organization: acme-co
  hostname: https://be-vv-fwi-694-cre.k8s.insights.fairwinds.com

  # You may need to set this to 'main'
  baseBranch: main

# These images will be scanned for vulnerabilities.
images:
  docker:
  - nginx:1.18-alpine

# These manifests will be scanned for configuration issues.
manifests:
  yaml:
  - ./deploy/manifests/
  - ./main.deployment.yaml
  helm:
  - name: prod
    path: ./deploy/chart
    values:
      foo: bar

options:
  organization: acme-co
  hostname: https://be-vv-change.k8s.insights.fairwinds.com

  # You may need to set this to 'main'
  baseBranch: main

manifests:
  yaml:
    - ./passing.yaml
    - ./failing.yaml

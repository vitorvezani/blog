# These images will be scanned for vulnerabilities.
images:
  docker:
  - nginx:1.18-alpine

manifests:
  yaml:
    - ./failing.yaml
    - ./passing.yaml
  helm:
    - name: staging
      path: ./chart/
      valuesFile: ./chart/values.yaml

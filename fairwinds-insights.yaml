options:
  organization: acme-co
  hostname: https://be-vv-fwi-1233-cr.k8s.insights.fairwinds.com
  junitOutput: ./junit/output.xml

  # You may need to set this to 'main'
  baseBranch: main

# These images will be scanned for vulnerabilities.
images:
  docker:
  - nginx:1.18-alpine

manifests:
  yaml:
    - ./failing.yaml
    - ./passing.yaml
